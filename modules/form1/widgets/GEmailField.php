<?php
class GEmailField extends GTextField {
	
	/*
	 * public function getRules() {
	 * $rules = $this->_rules;
	 * $this->_rules = array_merge($rules, array('email'));
	 * return parent::getRules();
	 * }
	 */
}

?>
